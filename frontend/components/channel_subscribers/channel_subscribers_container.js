import { connect } from 'react-redux';
import ChannelFriends from './channel_friends';

const mapStateToProps = state => ({
});

const mapDispatchToProps = dispatch => ({
});

export default connect(
  mapStateToProps,
  mapDispatchToProps
)(ChannelFriends);
